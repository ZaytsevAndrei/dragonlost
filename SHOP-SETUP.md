# 🛒 Настройка магазина и системы инвентаря

## 📋 Что было добавлено

### Backend:
1. **Таблицы БД** (включены в `backend/database/init.sql`):
   - `shop_items` - предметы магазина (с тестовыми предметами)
   - `player_inventory` - инвентарь игроков
   - `player_balance` - баланс игроков
   - `transactions` - история транзакций

2. **API Endpoints**:
   - `/api/shop/items` - список предметов магазина
   - `/api/shop/balance` - баланс игрока
   - `/api/shop/purchase` - покупка предмета
   - `/api/inventory` - инвентарь игрока
   - `/api/inventory/check-online` - проверка онлайн статуса
   - `/api/inventory/use/:id` - использование предмета

### Frontend:
1. **Новые страницы**:
   - `/shop` - магазин предметов
   - `/inventory` - инвентарь игрока

2. **Обновленные компоненты**:
   - Header - добавлено выпадающее меню с доступом к инвентарю

## 🚀 Установка

### 1. Инициализация базы данных

Все необходимые таблицы магазина включены в основной скрипт инициализации `backend/database/init.sql`.

Если база данных уже создана, но таблицы магазина отсутствуют, выполните:

```bash
# Запустите из корня проекта
npx ts-node -r dotenv/config backend/src/scripts/setupDatabase.ts
```

Или вручную через MySQL:
```bash
# Windows PowerShell
mysql -h ВАШ_ХОСТ -u ВАШ_ПОЛЬЗОВАТЕЛЬ -p ВАШ_ПАРОЛЬ dragonlost_web < backend/database/init.sql

# Linux/Mac
mysql -h ВАШ_ХОСТ -u ВАШ_ПОЛЬЗОВАТЕЛЬ -p ВАШ_ПАРОЛЬ dragonlost_web < backend/database/init.sql
```

### 2. Перезапустить сервер (если требуется)

```bash
npm run dev
```

**Примечание:** Таблицы и тестовые предметы создаются автоматически при первой инициализации базы данных.

## 📦 Предметы магазина

В базу данных добавлены следующие категории предметов:

### 🔫 Оружие
- AK-47 (150 монет)
- LR-300 (200 монет)
- Bolt Action Rifle (180 монет)
- Python Revolver (100 монет)
- Custom SMG (120 монет)

### 🛡️ Броня
- Hazmat Suit (80 монет)
- Metal Facemask (60 монет)
- Metal Chest Plate (70 монет)
- Heavy Plate Armor (250 монет)

### 🔨 Инструменты
- Jackhammer (90 монет)
- Chainsaw (100 монет)

### 📦 Ресурсы
- Metal Fragments x1000 (50 монет)
- High Quality Metal x100 (80 монет)
- Sulfur x1000 (60 монет)
- C4 x5 (300 монет)
- Rocket x10 (400 монет)

### 💊 Медикаменты
- Medical Syringe x5 (40 монет)
- Large Medkit x3 (35 монет)

### 🎁 Наборы
- Starter Kit (250 монет)
- Raider Kit (500 монет)
- Builder Kit (300 монет)

## 💰 Система баланса

По умолчанию у всех игроков баланс = 0.

### Как начислить монеты игроку:

```sql
-- Найти ID пользователя по Steam ID
SELECT id, username, steamid FROM users WHERE steamid = 'STEAM_ID_ИГРОКА';

-- Добавить баланс
INSERT INTO player_balance (user_id, balance, total_earned) 
VALUES (USER_ID, 1000.00, 1000.00)
ON DUPLICATE KEY UPDATE 
  balance = balance + 1000.00,
  total_earned = total_earned + 1000.00;

-- Или обновить существующий баланс
UPDATE player_balance 
SET balance = balance + 1000.00, 
    total_earned = total_earned + 1000.00 
WHERE user_id = USER_ID;
```

## 🎮 Использование

### Для игроков:

1. **Авторизоваться** через Steam
2. **Перейти в магазин** - кнопка "Магазин" в навигации
3. **Выбрать предметы** и купить (если есть баланс)
4. **Открыть инвентарь** - клик по имени пользователя → "Мой инвентарь"
5. **Зайти на сервер** в игре Rust
6. **Получить предметы** - в инвентаре нажать "Получить предмет"

### Важно:
- ⚠️ **Предметы можно получить только будучи онлайн на сервере!**
- Система проверяет активность игрока в базе PlayerDatabase
- Игрок считается онлайн, если активность была менее 5 минут назад

## 🔧 Интеграция с RCON

В файле `backend/src/routes/inventory.ts` есть TODO для интеграции с RCON:

```typescript
// TODO: Здесь должна быть интеграция с RCON для выдачи предмета в игре
// Пример: await executeRCONCommand(`inventory.giveto ${steamid} ${item.rust_item_code} ${item.quantity}`);
```

Для полной функциональности необходимо:
1. Установить RCON клиент для Rust
2. Реализовать функцию `executeRCONCommand`
3. Настроить подключение к Rust серверу

## 📝 Примеры использования API

### Получить все предметы магазина
```javascript
GET /api/shop/items
// Фильтр по категории
GET /api/shop/items?category=weapon
```

### Получить баланс
```javascript
GET /api/shop/balance
// Требуется авторизация
```

### Купить предмет
```javascript
POST /api/shop/purchase
{
  "item_id": 1,
  "quantity": 1
}
```

### Получить инвентарь
```javascript
GET /api/inventory
// Требуется авторизация
```

### Проверить онлайн статус
```javascript
GET /api/inventory/check-online
// Требуется авторизация
```

### Использовать предмет
```javascript
POST /api/inventory/use/:id
// Требуется авторизация и игрок должен быть онлайн
```

## 🎨 Интерфейс

### Выпадающее меню пользователя:
- Клик по имени пользователя открывает меню
- "🎒 Мой инвентарь" - переход в инвентарь
- "🚪 Выход" - выход из аккаунта

### Статус онлайн в инвентаре:
- 🟢 Зеленый - игрок онлайн (можно получать предметы)
- 🔴 Красный - игрок оффлайн (предметы получить нельзя)

## ⚙️ Дальнейшая разработка

1. **Автоматическое начисление монет** за игровое время
2. **Ежедневные бонусы**
3. **Реферальная система**
4. **Промокоды**
5. **Скидки и акции**
6. **Рейтинг покупателей**

## 🐛 Устранение проблем

### Предметы не выдаются в игре
- Проверьте, онлайн ли игрок на сервере
- Убедитесь, что настроен RCON (см. раздел интеграции)

### Баланс не отображается
- Проверьте, авторизован ли пользователь
- Убедитесь, что таблица `player_balance` создана

### Предметы магазина не загружаются
- Проверьте, что таблицы созданы: `SHOW TABLES LIKE 'shop_%';`
- Убедитесь, что есть данные: `SELECT COUNT(*) FROM shop_items;`
- Если таблицы пустые, выполните скрипт инициализации (см. раздел "Установка")
- Убедитесь, что backend запущен и доступен на порту 5000
- Проверьте консоль браузера на наличие ошибок API
