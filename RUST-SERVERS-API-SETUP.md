# Интеграция с Rust-Servers.net API

## ✅ Что сделано

1. **Установлены зависимости**
   - `node-fetch@2` - для HTTP запросов
   - `@types/node-fetch@2` - типы для TypeScript

2. **Создан сервис API** (`backend/src/services/rustServersApi.ts`)
   - Получение деталей сервера
   - Проверка голосований игроков
   - Отметка наград как полученных

3. **Обновлен роут серверов** (`backend/src/routes/servers.ts`)
   - Интеграция с rust-servers.net API
   - Fallback на базовые данные при ошибке
   - Трансформация данных в формат приложения

4. **Обновлены типы**
   - Backend: `backend/src/types/index.ts`
   - Frontend: `frontend/src/types/index.ts`
   - Добавлены поля: `country`, `rank`, `uptime`
   - `mapSize` и `lastWipe` теперь опциональны

5. **Обновлен компонент Servers** (`frontend/src/pages/Servers.tsx`)
   - Корректная обработка опциональных полей
   - Отображение рейтинга и uptime
   - Обработка null значений

## 🔧 Настройка

### 1. Добавьте API ключ в .env

Откройте (или создайте) файл `.env` в корне проекта и добавьте:

```env
RUST_SERVERS_API_KEY=npQdCNp9YXHZEYXb39jgz9YQ5wVNnaGJRJM
```

### 2. Перезапустите backend

```bash
cd backend
npm run dev
```

При запуске вы должны увидеть:
```
✅ Environment variables loaded
...
RUST_SERVERS_API_KEY: ✅ Set
```

### 3. Проверьте работу API

Откройте в браузере или Postman:
```
http://localhost:5000/api/servers
```

Вы должны получить реальные данные о вашем сервере:
```json
{
  "servers": [
    {
      "id": 123,
      "name": "DragonLost | Main Server",
      "ip": "185.189.255.19",
      "port": 35500,
      "players": 15,
      "maxPlayers": 100,
      "map": "Procedural Map",
      "status": "online",
      "lastWipe": "2026-01-10T00:00:00Z",
      "country": "RU",
      "rank": 1234,
      "uptime": 99.5
    }
  ]
}
```

## 📊 Что получаем от API

- **Реальный онлайн** - текущее количество игроков
- **Название сервера** - как оно отображается на rust-servers.net
- **Карта** - название текущей карты
- **Дата вайпа** - последний вайп сервера
- **Статус** - онлайн/оффлайн
- **Рейтинг** - позиция на rust-servers.net
- **Uptime** - процент времени работы
- **Страна** - где хостится сервер

## 🎁 Дополнительные возможности

### Система голосований

Вы можете добавить систему наград за голосование на rust-servers.net:

```typescript
// Проверить, проголосовал ли игрок
const voteStatus = await rustServersApi.checkVote(steamId);

if (voteStatus === 1) {
  // Игрок проголосовал, но не получил награду
  // Выдать награду игроку
  
  // Отметить награду как полученную
  await rustServersApi.claimVote(steamId);
}
```

**Статусы голосования:**
- `0` - игрок не голосовал
- `1` - проголосовал, награда не получена
- `2` - проголосовал, награда уже получена

## 🔍 Отладка

### Если данные не загружаются

1. **Проверьте API ключ** в `.env`
2. **Проверьте логи backend** - должно быть `RUST_SERVERS_API_KEY: ✅ Set`
3. **Проверьте консоль backend** на наличие ошибок от rust-servers.net API
4. **Убедитесь**, что ваш сервер добавлен на rust-servers.net и активен

### Если API возвращает ошибку

API автоматически вернет fallback данные:
```json
{
  "servers": [
    {
      "id": 1,
      "name": "DragonLost | Main Server",
      "ip": "185.189.255.19",
      "port": 35500,
      "players": 0,
      "maxPlayers": 100,
      "map": "Unknown",
      "status": "offline",
      "lastWipe": null
    }
  ]
}
```

## 📚 Документация

- **Rust-Servers.net API**: https://rust-servers.net/help/api/
- **Ваш API ключ**: https://rust-servers.net/server/<your-server-id>/
- **Сервис**: `backend/src/services/rustServersApi.ts`
- **Подробная документация**: `backend/src/services/README.md`

## 🚀 Что дальше?

1. ✅ API интегрирован и готов к использованию
2. Можете добавить систему наград за голосования
3. Можете добавить кэширование на стороне фронтенда для уменьшения нагрузки
4. Можете добавить автообновление данных каждые N секунд

---

**Готово!** Ваш сайт теперь показывает реальный онлайн с вашего Rust сервера через rust-servers.net API! 🎉
