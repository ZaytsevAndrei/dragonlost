# 🎮 Быстрый старт: Rust-Servers.net API

## ✅ Интеграция завершена!

Ваш сайт DragonLost теперь использует реальные данные от **rust-servers.net API** вместо моковых данных.

## 🚀 Что нужно сделать (2 минуты)

### 1. Добавьте API ключ в .env файл

Откройте (или создайте) файл **`.env`** в корне проекта (`f:\Projects\dragonlost\.env`) и добавьте строку:

```env
RUST_SERVERS_API_KEY=npQdCNp9YXHZEYXb39jgz9YQ5wVNnaGJRJM
```

### 2. Перезапустите backend

```bash
cd backend
npm run dev
```

### 3. Готово! 🎉

Откройте в браузере раздел "Серверы" и вы увидите:
- ✅ Реальный онлайн игроков
- ✅ Актуальное название сервера
- ✅ Текущую карту
- ✅ Дату последнего вайпа
- ✅ Рейтинг на rust-servers.net
- ✅ Uptime сервера

## 📊 Что изменилось?

### Backend
- ✅ Установлен `node-fetch` для HTTP запросов
- ✅ Создан сервис `backend/src/services/rustServersApi.ts`
- ✅ Обновлен роут `/api/servers` для использования реального API
- ✅ Добавлен fallback на базовые данные при ошибке

### Frontend
- ✅ Обновлены типы для новых полей
- ✅ Компонент Servers теперь показывает рейтинг и uptime
- ✅ Корректная обработка отсутствующих данных

### Новые поля в ответе API:
```typescript
{
  id: number;           // ID сервера на rust-servers.net
  name: string;         // Название сервера
  ip: string;           // IP адрес
  port: number;         // Порт
  players: number;      // Текущий онлайн
  maxPlayers: number;   // Максимум игроков
  map: string;          // Название карты
  status: 'online' | 'offline';  // Статус
  lastWipe: string | null;       // Дата вайпа
  country?: string;     // Страна (RU, US, etc)
  rank?: number;        // Рейтинг на rust-servers.net
  uptime?: number;      // Uptime в процентах
}
```

## 🔍 Проверка

### Тест API напрямую:
```bash
# В браузере или curl:
http://localhost:5000/api/servers
```

### Ожидаемый результат:
```json
{
  "servers": [
    {
      "id": 123,
      "name": "Ваше название сервера",
      "ip": "185.189.255.19",
      "port": 35500,
      "players": 15,
      "maxPlayers": 100,
      "status": "online",
      ...
    }
  ]
}
```

## 🎁 Бонус: Система голосований

API rust-servers.net также поддерживает систему голосований. Вы можете награждать игроков за голосование за ваш сервер!

**Пример использования:**
```typescript
import { rustServersApi } from './services/rustServersApi';

// Проверить голос игрока
const voteStatus = await rustServersApi.checkVote(steamId);
// 0 = не голосовал
// 1 = проголосовал, награда не получена
// 2 = проголосовал, награда получена

// Отметить награду как полученную
await rustServersApi.claimVote(steamId);
```

## 📚 Документация

- **Полная инструкция:** `RUST-SERVERS-API-SETUP.md`
- **Техническая документация:** `backend/src/services/README.md`
- **Rust-Servers.net API:** https://rust-servers.net/help/api/

## ❓ Проблемы?

1. **Не работает API?**
   - Проверьте, что `.env` файл создан в корне проекта
   - Проверьте, что переменная `RUST_SERVERS_API_KEY` установлена
   - Перезапустите backend после добавления `.env`

2. **Показывает offline?**
   - Убедитесь, что сервер активен на rust-servers.net
   - Проверьте консоль backend на наличие ошибок

3. **Данные не обновляются?**
   - API rust-servers.net кэширует данные ~3 минуты
   - Обновите страницу через несколько минут

---

**Готово!** Теперь ваш сайт показывает живые данные! 🚀
